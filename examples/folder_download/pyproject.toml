[project]
name = "example-folder-download"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "labapi[dotenv,builtin-auth]",
]

[tool.uv.sources]
labapi = { path = "../../", editable = true }
